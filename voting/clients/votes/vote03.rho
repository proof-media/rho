
new ack, log(`rho:io:stdout`) in {
  for (@msg <= ack) { log!("Received ACK: " ++ msg) }
  |
  @["Proof", "CastVote"]!("user003", "true", *ack)
  |
  @["Proof", "CastVote"]!("user003", "true", *ack)
  |
  @["Proof", "CastVote"]!("user003", "true", *ack)
}
